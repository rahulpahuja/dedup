<AppFunctionAppMetadata xmlns:appfn="http://schemas.android.com/apk/androidx.appfunctions"
    appfn:description="DeDup finds and cleans up duplicate photos/videos, large files, and
    junk on the device. Operational Patterns:
    - Use 'findLargeFiles' to find the largest photos/videos/audio files on the device,
    optionally filtered by a minimum size.
    Constraints:
    - All exposed functions are read-only; DeDup does not expose file deletion to agents."
    appfn:displayDescription="@string/app_functions_display_description" />
